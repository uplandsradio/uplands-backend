export async function loginUser(req, res) {
  return res.json({ message: "Login success (placeholder)" });
}

export async function registerUser(req, res) {
  return res.json({ message: "Register success (placeholder)" });
}